# Règles pour les agents

- Toujours créer les fichiers d'explications et les artefacts au format Markdown (.md) directement à la racine du workspace de l'utilisateur (dossier courant `/home/gabrielvf/Public`), plutôt que dans les dossiers internes de l'application (comme `.gemini`), afin qu'ils soient facilement accessibles et visibles pour l'utilisateur depuis son terminal.
